apiVersion: v1
kind: Service
metadata:
  name: memory-limiter-service
  namespace: resource-limiter
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: memory-limiter
